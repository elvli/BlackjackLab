import React from "react";
import SettingsModal from "./SettingsModal";

const ProductCard = () => {
  return (
    <div>
      ProductCard
      <SettingsModal />
    </div>
  );
};

export default ProductCard;
